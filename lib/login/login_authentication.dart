import 'package:flutter/material.dart';

class LAuhentication extends StatelessWidget {
  const LAuhentication({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('AUTHENTİCATİON'),
    );
  }
}
